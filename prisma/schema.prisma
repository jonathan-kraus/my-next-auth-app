generator client {
  provider = "prisma-client-js"
  output   = "../src/generated/"
  engineType = "library"
}

datasource db {
  provider = "postgresql"
  schemas  = ["neon_auth", "public"]
}

model jtemp {
  id          Int      @id @default(autoincrement())
  name        String
  email       String
  created_at  DateTime @default(now())

  @@map("jtemp") // Ensures Prisma uses the lowercase table name
  @@schema("public")
}

model User {
  id             String   @id @default(cuid())
  name           String?
  email          String?  @unique
  emailVerified  DateTime?
  image          String?
  accounts       Account[]
  sessions       Session[]
  logs           Log[]
  @@schema("public")
}

// Your Log model, updated to include a User relationship
model Log {
  id        String   @id @default(uuid()) // Using uuid for Log IDs
  userId    String   // 🎯 NEW: Foreign key to User
  severity  String
  source    String   
  message   String
  requestId String?
  metadata  Json?
  timestamp DateTime @default(now())

  // Relationship to the User model
  user      User     @relation(fields: [userId], references: [id])
  @@schema("public")
}

model Account {
  id                String   @id @default(cuid())
  userId            String
  type              String
  provider          String
  providerAccountId String
  refresh_token     String?
  access_token      String?
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String?
  session_state     String?

  user User @relation(fields: [userId], references: [id])

  @@unique([provider, providerAccountId])

  @@schema("public")
}

model Session {
  id           String   @id @default(cuid())
  sessionToken String   @unique
  userId       String
  expires      DateTime

  user User @relation(fields: [userId], references: [id])

@@schema("public")
}

model VerificationToken {
  identifier String
  token      String   @unique
  expires    DateTime

  @@unique([identifier, token])

  @@schema("public")
}
